# Signing secret for agent credentials. Use a long random string.
# In production, source this from a KMS / secrets manager and rotate it on a schedule.
AGENT_SIGNING_SECRET=change-me-to-a-long-random-string
